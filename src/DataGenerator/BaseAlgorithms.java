package datagenerator;

public class BaseAlgorithms {
    public static int[] revertData(int[] array, int size) {
        int[] revertedArray = new int[size];

        for(int i=0; i <= array.length; i++)

        
        return array;
    }
}
